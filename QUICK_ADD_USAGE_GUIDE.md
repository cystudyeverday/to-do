# Quick Add 批量输入功能使用指南

## 🚀 快速开始

### 步骤 1: 打开 Quick Add
1. 在任务管理页面点击 "Quick Add" 按钮
2. 或者使用快捷键（如果有的话）

### 步骤 2: 选择批量模式
1. 在模态框中找到 "Input Mode" 部分
2. 点击 "Batch Input" 按钮（蓝色高亮表示已选中）

### 步骤 3: 选择项目
1. 在 "Project" 下拉框中选择一个项目
2. 如果没有项目，请先创建一个项目

### 步骤 4: 输入任务列表
在 "Batch Input" 文本框中输入你的任务列表，支持以下格式：

#### 格式 1: 带编号的列表
```
1.re-gen topic
2.compliance matrix query / edit
3.valid option now has a showSendBtn boolean
data source view only
4.user management view only view for non-CMP user
```

#### 格式 2: 不带编号的列表
```
re-gen topic
compliance matrix query / edit
valid option now has a showSendBtn boolean
data source view only
user management view only view for non-CMP user
```

#### 格式 3: 混合格式
```
1. 第一个任务
不带编号的任务
3. 另一个带编号的任务
最后一个任务
```

### 步骤 5: 预览和确认
1. 查看实时预览区域，确认任务数量和内容
2. 检查每个任务的模块分类是否正确
3. 点击 "Add X Tasks" 按钮批量添加

## ✅ 成功示例

### 输入内容
```
1.re-gen topic
2.compliance matrix query / edit
3.valid option now has a showSendBtn boolean
data source view only
4.user management view only view for non-CMP user
```

### 预期结果
系统会显示预览：
```
Preview (5 tasks)
• re-gen topic
• compliance matrix query / edit
• valid option now has a showSendBtn boolean
• data source view only
• user management view only view for non-CMP user
```

### 最终创建的任务
1. **re-gen topic** → Content Management 模块
2. **compliance matrix query / edit** → Compliance 模块
3. **valid option now has a showSendBtn boolean** → Configuration 模块
4. **data source view only** → Data Source 模块
5. **user management view only view for non-CMP user** → User Management 模块

## ⚠️ 常见问题和解决方案

### 问题 1: "Please enter batch input and select project"
**原因**: 没有输入任务列表或没有选择项目
**解决方案**:
1. 确保在批量输入文本框中输入了任务列表
2. 确保在项目下拉框中选择了一个项目
3. 检查输入格式是否正确

### 问题 2: "No valid tasks found"
**原因**: 输入格式不正确或只包含空行
**解决方案**:
1. 确保每行都有任务内容
2. 检查是否有多余的空行
3. 确保任务内容不为空

### 问题 3: 项目下拉框为空
**原因**: 没有创建任何项目
**解决方案**:
1. 先创建一个项目
2. 刷新页面
3. 重新打开 Quick Add 模态框

### 问题 4: 模块分类不正确
**原因**: 任务内容与关键词不匹配
**解决方案**:
1. 检查任务描述是否包含相关关键词
2. 可以手动调整模块分类
3. 参考支持的模块列表

## 📋 支持的模块分类

| 模块 | 关键词示例 |
|------|------------|
| **Compliance** | compliance, matrix, query, edit, regulation, policy, standard, audit |
| **User Management** | user, management, role, permission, access, control, rbac, non-cmp |
| **Data Source** | data source, datasource, data management, data view, view only |
| **Configuration** | config, configuration, option, valid option, showSendBtn, boolean |
| **Content Management** | content, topic, re-gen, regeneration, content management |
| **Frontend** | ui, ux, interface, react, vue, angular, component, page, screen |
| **Backend** | api, server, backend, service, controller, route, endpoint |
| **Database** | database, db, table, schema, migration, query, sql |
| **Testing** | test, testing, unit, integration, e2e, jest, mocha, cypress |
| **Security** | security, auth, authentication, authorization, login, password |
| **DevOps** | deploy, deployment, ci, cd, pipeline, docker, kubernetes |
| **UI/UX** | ui, ux, design, user experience, interface, wireframe |
| **Other** | 其他不匹配的内容 |

## 🎯 最佳实践

### 1. 任务描述
- 使用清晰、具体的描述
- 包含关键功能词汇
- 避免过于简短的描述

### 2. 输入格式
- 每行一个任务
- 可以带编号或不带编号
- 支持混合格式
- 自动过滤空行

### 3. 项目选择
- 确保选择正确的项目
- 批量任务会添加到同一个项目
- 可以后续调整项目分配

### 4. 模块分类
- 系统会自动分类
- 可以后续手动调整
- 基于关键词匹配

## 🔧 高级技巧

### 1. 快速输入
```
1. 复制任务列表到剪贴板
2. 切换到批量输入模式
3. 粘贴到文本框
4. 检查预览
5. 点击添加
```

### 2. 格式优化
- 使用一致的编号格式
- 保持描述简洁明了
- 包含必要的技术词汇

### 3. 批量处理
- 一次性添加多个相关任务
- 利用智能分类功能
- 提高工作效率

## 📊 性能提示

1. **大量任务**: 建议一次添加不超过 20 个任务
2. **复杂描述**: 系统会自动处理长描述
3. **实时预览**: 输入时即时显示解析结果
4. **错误处理**: 自动过滤无效输入

## 🆘 获取帮助

如果遇到问题：

1. **检查控制台**: 打开浏览器开发者工具查看错误信息
2. **验证输入**: 确保输入格式正确
3. **刷新页面**: 重新加载页面
4. **联系支持**: 如果问题持续存在

## 📝 示例模板

### 企业应用开发模板
```
1. 用户认证和授权系统
2. 数据源管理和配置
3. 合规性检查和报告
4. 用户角色权限管理
5. 系统配置和参数设置
6. 内容管理和生成
7. 前端界面优化
8. 后端API开发
9. 数据库设计和优化
10. 安全漏洞修复
```

### 功能开发模板
```
1. 实现用户登录功能
2. 添加数据验证逻辑
3. 创建用户管理界面
4. 配置系统参数
5. 生成内容模板
6. 优化页面性能
7. 修复已知问题
8. 添加单元测试
9. 部署到生产环境
10. 监控系统状态
```

使用这些模板可以快速创建标准化的任务列表！ 